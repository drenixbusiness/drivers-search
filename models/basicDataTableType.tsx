/**
 * Kept for backwards compatibility — the driver models now live in models/driver.ts,
 * where a row is a merged profile instead of a single Monday item.
 */
export type {
    ApplicationOutcome,
    DriverApplication,
    DriverProfile,
    DriverSearchResponse,
    DriverStats,
    DriversFilters,
} from './driver';
